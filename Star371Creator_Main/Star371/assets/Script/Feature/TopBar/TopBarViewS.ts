import { _decorator, Component, Node, Sprite, SpriteFrame } from 'cc';
import { TopBarView } from './TopBarView';
const { ccclass, property } = _decorator;

@ccclass('TopBarViewS')
export class TopBarViewS extends TopBarView {
    protected override start(): void {
        super.start?.();
    }
}


